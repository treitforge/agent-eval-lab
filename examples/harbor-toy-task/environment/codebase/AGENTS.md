# Scan Ledger instructions

This is a small Python teaching project.

Run the tests with:

```bash
python -m unittest discover -s tests -v
```

Keep identifier normalization in `normalize_scan_id`. Preserve the public API and first-seen order. Use only the Python standard library.
